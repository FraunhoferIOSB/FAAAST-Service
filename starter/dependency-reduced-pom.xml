<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <artifactId>service</artifactId>
    <groupId>de.fraunhofer.iosb.ilt.faaast.service</groupId>
    <version>${revision}</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>starter</artifactId>
  <name>starter</name>
  <version>${revision}</version>
  <description>Starter for the FA³ST Service.</description>
  <build>
    <plugins>
      <plugin>
        <groupId>com.google.cloud.tools</groupId>
        <artifactId>jib-maven-plugin</artifactId>
        <version>${plugin.jib.version}</version>
        <configuration>
          <to>
            <image>${container.image.name}:${container.image.tag}</image>
            <auth>
              <username>${env.DOCKER_USERNAME}</username>
              <password>${env.DOCKER_TOKEN}</password>
            </auth>
          </to>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven-compiler-plugin-version}</version>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>info.picocli</groupId>
              <artifactId>picocli-codegen</artifactId>
              <version>${picoli-plugin-version}</version>
            </path>
          </annotationProcessorPaths>
          <compilerArgs>
            <arg>-Aproject=${project.groupId}/${project.artifactId}</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven-shade-plugin-version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <mainClass>de.fraunhofer.iosb.ilt.faaast.service.starter.Application</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <artifactId>hamcrest-core</artifactId>
          <groupId>org.hamcrest</groupId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>
  <properties>
    <root.basedir>${project.parent.basedir}</root.basedir>
    <maven-shade-plugin-version>3.2.4</maven-shade-plugin-version>
    <maven-compiler-plugin-version>3.9.0</maven-compiler-plugin-version>
    <picoli-plugin-version>4.6.2</picoli-plugin-version>
    <logback.version>1.2.10</logback.version>
    <maven-assembly-plugin.version>3.3.0</maven-assembly-plugin.version>
  </properties>
</project>
